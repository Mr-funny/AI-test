function generatePhoneNumber(countryCode) {
    // Implement the logic to generate a phone number for the specified country
    // You can redirect to a new page with the generated phone number
    window.location.href = `${countryCode}-phone-number-generator`;
}
